programa {
  funcao inicio() {

    /*4) Um festival de balonismo oferece passeios de balão. 
    Para cada 5 minutos de voo, são necessários 10m³ (metros cúbicos) de gás, sendo que o metro cúbico de gás custa R$15 reais. 
    No balão cabem no máximo 4 pessoas. 
    O cálculo do valor do passeio é feito somando o valor gasto em gás, mais uma taxa de R$20 reais por pessoa. 
    Criar um programa que pergunte quantas pessoas vão no passeio e o tempo de passeio. 
    Mostrar na tela o total cobrado pelo passeio.*/

    //Definição de variáveis

    inteiro valorMetroCubicoGas = 15, taxaPorPessoa = 20, gasACadaCincoMinutos = 10
    inteiro pessoasPorBalao = 4, minutosPorCadaDezMetrosCubicos = 5
    inteiro quantidadePessoas, duracaoPasseio, valorTotalPasseio, valorGasACadaCincoMinutos

    //Entrada de dados

    escreva ("Informe a quantidade de pessoas: ")
    leia (quantidadePessoas)

    escreva ("Informe a duração do passeio (em min): ")
    leia (duracaoPasseio)

    //Cálculo/Lógica

   valorGasACadaCincoMinutos = valorMetroCubicoGas * gasACadaCincoMinutos
   
   
    valorTotalPasseio = (quantidadePessoas*taxaPorPessoa) + (valorGasACadaCincoMinutos*duracaoPasseio/minutosPorCadaDezMetrosCubicos)

    //Saída de dados

    escreva ("\nTotal de pessoas no passeio : ", quantidadePessoas)
  
    escreva ("\nValor total + taxa por pessoa (", taxaPorPessoa, ") : ", valorTotalPasseio)
    leia (valorTotalPasseio)
    
  }
}
